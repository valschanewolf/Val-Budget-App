@model BudgetApp.Models.Household
@{
	ViewBag.Title = "Join";
}

<h2>@ViewBag.Title.</h2>

@using (Html.BeginForm("Join", "Households", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h4>Join the @ViewBag.HouseholdName household.<br />With the code: @ViewBag.Code</h4>
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })
	<h5>@Model.Id<br />
	@Model.HouseholdName<br />
	</h5>
	
    @*<div class="form-group">
        @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
        </div>
    </div>*@
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Register" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
